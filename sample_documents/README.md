# Curated sample document set

These short documents are demonstration extracts created from public scheme information and point to the official portals. They are not replacements for current notifications, detailed guidelines, bank appraisal, or government approval. The `last_verified` date is stored in `data/schemes.json`.

Run `python scripts/ingest_documents.py` to rebuild the local vector index from the database-backed scheme records.
